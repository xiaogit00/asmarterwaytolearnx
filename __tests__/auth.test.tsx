// The first test is sending a request to login with a pair of user credentials and getting a JWT. 
// but in this case, I'll need to leverage on Oauth or something. Let's see how that can be done. 

// it("Can get JWT token upon successful login", () => {
    
// })